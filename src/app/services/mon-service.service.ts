import { Injectable } from '@angular/core';

@Injectable({
 providedIn: 'root', // Le service est disponible dans toute l'application (Singleton)
})
export class MonService {
 // Vos données et méthodes ici
}
